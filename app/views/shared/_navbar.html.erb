<nav class="navbar navbar-expand-lg navbar-dark border-bottom fixed-top" style="background-color: black;">
    <div class="container-fluid">
        <%= link_to root_path, class: 'navbar-brand' do %>
            <div class="text-light text-center font-weight-bold">
                <%= svg('twitter') %>
            </div>
        <% end %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <% if user_signed_in? %>
                    <li class="nav-item">
                        <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'nav-link active' %>
                    </li>
                <% else %>
                    <li class="nav-item">
                        <%= link_to 'Sign up', new_user_registration_path, class: 'nav-link active font-weight-bold' %>
                    </li>
                    <li class="nav-item">
                        <%= link_to 'Log in', new_user_session_path, class: 'nav-link active' %>
                    </li>
                <% end %>
            </ul>
            <div class="container text-light font-weight-bold">
                <div class="notice">
                    <%= notice %>
                </div>
                <div class="alert">
                    <%= alert %>
                </div>
            </div>
        </div>
    </div>
</nav>